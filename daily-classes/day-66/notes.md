# Day-66 - Befor React we should know these topic ---

1. Basic of JS
- var let const
- datatypes
- operators
- console statement
- conditions
- loops

2. Arrays Object
- Methods of Array (push, pop, shift, unshift, length)
- HOF (forEach, map, filter, reduce, some, find)
- basic level question solving with array
- use of objects
- Array of objects
- basic questions of array of objects

3. Destructuring
- of array
- of object
- spread operator
- rest operator

4. Function
- basis fnc
- arrow
- return statement
- parameter, argument

5. Promises, Async Await, fetch api

6. error handling

7. import export of js

### Export

1. **default export **- 1 export - import name can be changed
2. **named export - **many export - import name can’t be changed

```
const skills = ['js’, ‘react’, ‘next’];

export default skills
```

```
export const name = 'Prashant';
export const city = 'Bokaro';
```

```
import king ./default.js;
import {user, city} ./named.js;

console.log(king);
console.log(user);
console.log(city);
```